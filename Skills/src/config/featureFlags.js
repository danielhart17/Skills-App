/**
 * Feature flags for temporarily archived or beta product surfaces.
 *
 * To unhide any flag: set it to `true`, save, and reload the app.
 *
 * LIVE_SHOOTING_SESSION_TRACKER — court tap tracker (athlete + parent).
 * AI_WORKOUT_BUILDER — "Build My Workout" AI flow (/ai-workout-builder).
 */
export const FEATURE_FLAGS = {
  LIVE_SHOOTING_SESSION_TRACKER: true,
  AI_WORKOUT_BUILDER: false,
};
